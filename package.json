{
  "name": "recipes",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A recipe archive rendered from markdown fixtures into a single self-contained HTML file.",
  "scripts": {
    "dev": "node src/serve.js",
    "build": "node src/build.js",
    "test": "node src/build.js && node test/verify.mjs && node test/interact.mjs",
    "shots": "node src/build.js && node test/shots.mjs",
    "convert": "node scripts/convert-fixtures.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^14.1.3"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
