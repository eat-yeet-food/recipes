# Codex Adapter

`CLAUDE.md` is the canonical agent guide for this repository. Read it first and
follow it as the source of truth for project workflow, traps, verification, and
deployment. This file exists only because Codex discovers `AGENTS.md`.

Do not duplicate guidance here. If an instruction should apply to future agents,
update `CLAUDE.md` so Claude and Codex inherit the same rules.
